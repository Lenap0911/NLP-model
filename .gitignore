# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
env/

# Embeddings: 227 MB — exceeds GitHub's 100 MB hard limit, cannot be committed
# Regenerate with: python run_nlp_pipeline.py (skip-embed flag reuses this once generated)
output/labse_embeddings.npy

# Complementary pipeline outputs — scraped data, regenerated per run
complementary/output/*.json
complementary/output/*.csv
complementary/analysis/reports/*.json
complementary/analysis/reports/*.csv

# Logs: machine-specific runtime noise, changes every run
logs/

# OS
.DS_Store
Thumbs.db
